import DisplayContainer from "../../UI/DisplayContainer/DisplayContainer";

const Recent = () => {
  return (
    <>
      <DisplayContainer
        heading="Recents"
        notFound="No recent chats"
        className="recent"
      />
    </>
  );
};

export default Recent;
